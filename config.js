
export const breakpoints = [
  '32em',
  '52em',
  '72em'
].map(b => `@media screen and (min-width: ${b})`)

const black = '#003c4a'
const blue = '#00b7e0'
const darkblue = '#007690'

export const colors = {
  black,
  blue,
  darkblue,
  primary: blue
}

