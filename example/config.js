
export const breakpoints = [
  '32em',
  '52em',
  '72em',
].map(b => `@media screen and (min-width: ${b})`)

const black = '#004640'
const blue = '#07c'
const blue2 = '#0081dd'
const cyan = '#0cb'

export const colors = {
  black,
  blue,
  blue2,
  cyan,
  primary: blue
}

