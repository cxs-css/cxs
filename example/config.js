
export const breakpoints = [
  '40em',
  '52em',
  '64em',
].map(b => `@media screen and (min-width: ${b})`)

const black = '#374047'
const blue = '#07c'
const cyan = '#0cb'

export const colors = {
  black,
  blue,
  cyan,
  primary: blue
}

